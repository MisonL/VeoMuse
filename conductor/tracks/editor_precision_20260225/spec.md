# Specification: 工匠之手计划

## 目标
深化编辑器的细节操作能力，实现专业级的片段属性调节和时间轴交互。

## 关键技术栈
- **属性联动**: 选中状态管理，双向绑定 Store 数据到 UI 表单。
- **时间轴缩放**: 动态计算 `Timeline` 组件的 `scale` 和 `step`。
- **右键菜单**: 阻止原生 ContextMenu，渲染自定义浮窗。

## 预期产出
- 新增“属性检查器”面板。
- 时间轴支持滑块缩放。
- 支持选中片段后按下 `Delete` 键删除。
